 <% @compare.headline%>
  <%= link_to @compare.headline, compare_path(@compare)  %>
  <div class="compare">
    <div class="comp1">
      <img src='<%= @compare.img1_url%>'><br>
      <button>vote</button>
    </div>
    <div class="comp2">
      <img src='<%= @compare.img2_url%>'><br>
      <button>vote</button>
    </div>
  </div>

 